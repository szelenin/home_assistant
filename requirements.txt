# Core dependencies for Home Assistant
PyYAML>=6.0
pyttsx3>=2.90
SpeechRecognition>=3.10.0
pyaudio>=0.2.11  # Requires PortAudio system dependency
sounddevice>=0.4.6  # For audio device management and TTS
# AI Provider dependencies
anthropic>=0.7.0  # For Claude AI integration (default provider)
openai>=1.0.0     # For ChatGPT integration

# TTS Provider dependencies
piper-tts>=1.3.0  # For Piper neural TTS
numpy>=1.21.0     # For Piper audio processing
soundfile>=0.10.0 # For Piper audio file handling

# Speech Recognition Provider dependencies
vosk>=0.3.45          # For Vosk offline speech recognition (default provider)
openai-whisper>=20231117  # For OpenAI Whisper speech recognition
# SpeechRecognition>=3.10.0 already included above for Google provider

# Wake Word Detection Provider dependencies
openwakeword>=0.6.0          # For OpenWakeWord detection (default provider)
pvporcupine>=3.0.0          # For Picovoice Porcupine wake word detection
pocketsphinx>=5.0.0         # For PocketSphinx keyword spotting

# Optional dependencies for advanced speech recognition
# google-cloud-speech  # For Google Cloud Speech-to-Text API
# pocketsphinx>=0.1.15 # For CMU Sphinx offline recognition (legacy)

# Development and testing dependencies
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=5.0.0